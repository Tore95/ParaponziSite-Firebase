// Initialize Firebase
// this js object has been censored
var config = {
    apiKey: "PRIVATE_API_KEY",
    authDomain: "PRIVATE_FIREBASE_DOMAIN",
    databaseURL: "PRIVATE_DATABASE_URL",
    projectId: "PRIVATE_PROJECT",
    storageBucket: "PRIVATE",
    messagingSenderId: "PRIVATE"
};

// Initialize Cloud Firestore through Firebase
firebase.initializeApp(config);